{
  // Visit https://aka.ms/tsconfig to read more about this file
  "compilerOptions": {
    // File Layout
    "rootDir": "./src",
    "outDir": "./dist",

    // Environment Settings
    // See also https://aka.ms/tsconfig/module
    "module": "nodenext",
    "moduleResolution": "NodeNext",

    // "target": "ES2022": Es una especificación fija y estable. Le dices a TypeScript: "Quiero que mi código JavaScript final sea compatible con el estándar ECMAScript 2022". El compilador transpilará cualquier característica más nueva que uses (por ejemplo, de ES2023) a una sintaxis equivalente que existía en ES2022, pero dejará intactas todas las características de ES2022 (como async/await, campos de clase privados, top-level await).

    // "target": "ESNext": Es un objetivo móvil y experimental. Le dices a TypeScript: "Confío en ti. Compila mi código a la versión de JavaScript más moderna y vanguardista que conozcas, incluidas las características que aún están en propuesta (Stage 3+) y no se han estandarizado".

    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,

    "target": "esnext",
    "types": [],
    // For nodejs:
    // "lib": ["esnext"],
    // "types": ["node"],
    // and npm install -D @types/node

    // Other Outputs
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,

    // Stricter Typechecking Options
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,

    // Style Options
    // "noImplicitReturns": true,
    // "noImplicitOverride": true,
    // "noUnusedLocals": true,
    // "noUnusedParameters": true,
    // "noFallthroughCasesInSwitch": true,
    // "noPropertyAccessFromIndexSignature": true,

    // Recommended Options
    "strict": true,
    "jsx": "react-jsx",
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "noUncheckedSideEffectImports": true,
    "moduleDetection": "force",
    "skipLibCheck": true,
    "esModuleInterop": true, // Mejora la compatibilidad con módulos CommonJS
    "forceConsistentCasingInFileNames": true // Evita problemas entre OS
  },
  "include": ["src/**/*"]
}
